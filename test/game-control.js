const assert = require('chai').assert;
const State = require('../lib/game-control');

describe('State', function(){

  it('should be a function', function() {
    assert.isFunction(State);
  });
// it should build the grid

});
